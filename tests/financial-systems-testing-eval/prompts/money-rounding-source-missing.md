# Multi-Currency Wallet Rounding

A wallet service stores balances for USD and JPY. Its transfer code applies a fee after conversion, but the repository does not identify a rounding mode, source-of-truth currency scale, or rule for conversion residuals. Produce a read-only test strategy for this change request. Cite only facts that a project artifact would need to establish.
