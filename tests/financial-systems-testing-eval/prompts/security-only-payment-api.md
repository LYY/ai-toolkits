# Signed Payment API Request

An API path is named `/payment-events`, but it only validates request signatures, authorization scopes, and replay protection for incoming webhooks. It does not create charges, balances, transfers, or settlement records. Produce a read-only test strategy from these facts.
